'use client'

import { useEffect, useMemo, useState } from 'react'
import Shell from '../../components/Shell'
import CompanyScopeBar from '../../components/CompanyScopeBar'
import { Badge, Panel, Progress, StatCard } from '../../components/Ui'
import { AlertTriangle, CheckCircle2, Clock3, Download, ExternalLink, GraduationCap, Plus, Search, X } from 'lucide-react'
import { dbSelect, dbUpsert, getStoredProfile, isSupabaseConfigured } from '../../lib/supabase-rest'
import { COMPANY_MASTER, DEFAULT_COMPANY_FILTERS, companyCodeOf, filteredCompanies } from '../../lib/company-master'

const STORAGE_KEY='sinshe-learning-records'
const recordTypes=['SIO','Certification','Training']
const categories=['SIO Operator','Sertifikat Kompetensi K3','Sertifikat Kompetensi Lingkungan','Safety Induction','Technical Training','Emergency Response','Environmental','Leadership','Other']
const workStatuses=['Active','Inactive','Contractor','Mutation','Resigned']
const emptyForm={companyCode:'ACP',employeeName:'',employeeId:'',jobTitle:'',workStatus:'Active',unit:'',recordType:'SIO',trainingName:'',category:'SIO Operator',trainingDate:'',validUntil:'',competency:'',provider:'',certificateNo:'',documentLink:'',notes:'',mandatory:true}

function readLocal(){try{const raw=localStorage.getItem(STORAGE_KEY);return raw?JSON.parse(raw):[]}catch{return[]}}
function writeLocal(rows){try{localStorage.setItem(STORAGE_KEY,JSON.stringify(rows));window.dispatchEvent(new CustomEvent('sinshe-central-data',{detail:{key:STORAGE_KEY,count:rows.length}}))}catch{}}
function daysTo(value){if(!value)return null;const t=new Date();t.setHours(0,0,0,0);const d=new Date(`${value}T00:00:00`);return Math.ceil((d-t)/86400000)}
function derivedStatus(row){const d=daysTo(row.validUntil||row.valid_until);if(d===null)return row.status||'Valid';if(d<0)return'Expired';if(d<=30)return'Due Soon';return'Valid'}
function tone(status){return status==='Valid'?'green':status==='Due Soon'?'orange':'red'}
function fmt(value){return value?new Intl.DateTimeFormat('id-ID',{day:'2-digit',month:'short',year:'numeric'}).format(new Date(`${String(value).slice(0,10)}T00:00:00`)):'-'}
function fromDb(r){return{id:r.id,companyCode:r.company_code||'',employeeName:r.employee_name||'',employeeId:r.employee_id||'',jobTitle:r.job_title||'',workStatus:r.work_status||'Active',unit:r.unit||'',recordType:r.record_type||'Training',trainingName:r.training_name||'',category:r.category||'',trainingDate:r.training_date||'',validUntil:r.valid_until||'',status:r.status||'Valid',competency:r.competency||'',provider:r.provider||'',certificateNo:r.certificate_no||'',documentLink:r.document_link||'',notes:r.notes||'',mandatory:r.mandatory!==false}}
function toDb(r){return{id:r.id,company_code:r.companyCode,employee_name:r.employeeName,employee_id:r.employeeId||null,job_title:r.jobTitle||null,work_status:r.workStatus||'Active',unit:r.unit||'Unknown',record_type:r.recordType||'Training',training_name:r.trainingName,category:r.category,training_date:r.trainingDate||null,valid_until:r.validUntil||null,status:derivedStatus(r),competency:r.competency||null,provider:r.provider||null,certificate_no:r.certificateNo||null,document_link:r.documentLink||null,notes:r.notes||null,mandatory:r.mandatory!==false}}

export default function LearningCompetency(){
 const[items,setItems]=useState([]),[filters,setFilters]=useState(DEFAULT_COMPANY_FILTERS),[search,setSearch]=useState(''),[statusFilter,setStatusFilter]=useState('All'),[typeFilter,setTypeFilter]=useState('All'),[modal,setModal]=useState(false),[form,setForm]=useState(emptyForm),[notice,setNotice]=useState('')
 const profile=getStoredProfile(),canManage=!profile||profile.role!=='Viewer'
 useEffect(()=>{let active=true;(async()=>{let rows=[];if(isSupabaseConfigured()){try{rows=(await dbSelect('learning_records','select=*&order=updated_at.desc')).map(fromDb)}catch{rows=[]}}if(!rows.length)rows=readLocal();if(active){setItems(Array.isArray(rows)?rows:[]);writeLocal(Array.isArray(rows)?rows:[])}})();return()=>{active=false}},[])
 const companies=useMemo(()=>filteredCompanies(filters),[filters]),allowed=useMemo(()=>new Set(companies.map(c=>c.code)),[companies]),specific=filters.company!=='All'||filters.region!=='All'||filters.province!=='All'||filters.pic!=='All'
 const filtered=useMemo(()=>items.filter(row=>{const code=companyCodeOf(row),companyMatch=!code?!specific:allowed.has(code),q=search.trim().toLowerCase(),searchMatch=!q||[row.id,row.employeeName,row.employeeId,row.jobTitle,row.trainingName,row.category,row.unit,row.provider,row.certificateNo].join(' ').toLowerCase().includes(q),status=derivedStatus(row);return companyMatch&&searchMatch&&(statusFilter==='All'||status===statusFilter)&&(typeFilter==='All'||row.recordType===typeFilter)}),[items,specific,allowed,search,statusFilter,typeFilter])
 const valid=filtered.filter(r=>derivedStatus(r)==='Valid').length,dueSoon=filtered.filter(r=>derivedStatus(r)==='Due Soon').length,expired=filtered.filter(r=>derivedStatus(r)==='Expired').length,compliance=filtered.length?Math.round(valid/filtered.length*100):0,sioCount=filtered.filter(r=>r.recordType==='SIO').length,certCount=filtered.filter(r=>r.recordType==='Certification').length
 function flash(text){setNotice(text);setTimeout(()=>setNotice(''),3000)}
 function update(field,value){setForm(prev=>({...prev,[field]:value}))}
 async function save(e){e.preventDefault();if(!form.companyCode||!form.employeeName.trim()||!form.trainingName.trim()){flash('Lengkapi PT, nama karyawan, dan jenis SIO/sertifikat/training.');return}const seq=Math.max(0,...items.map(i=>Number(String(i.id||'').split('-').pop())||0))+1,item={id:`CMP-${String(seq).padStart(5,'0')}`,...form,status:'Valid'},next=[item,...items];setItems(next);writeLocal(next);if(isSupabaseConfigured()){try{await dbUpsert('learning_records',[toDb(item)],'id')}catch(err){flash(`Tersimpan lokal, sync Supabase gagal: ${err.message}`);return}}setForm({...emptyForm,companyCode:filters.company!=='All'?filters.company:'ACP'});setModal(false);flash(`${item.id} berhasil ditambahkan.`)}
 function exportCSV(){const header=['ID','Company/PT','Nama Karyawan','NIK','Jabatan','Status Kerja','Unit','Record Type','Jenis SIO/Sertifikat/Training','Kategori','No Sertifikat','Tanggal Terbit/Training','Berlaku Sampai','Sisa Hari','Status Masa Berlaku','Provider','Link Dokumen','Catatan'];const rows=filtered.map(r=>[r.id,r.companyCode,r.employeeName,r.employeeId,r.jobTitle,r.workStatus,r.unit,r.recordType,r.trainingName,r.category,r.certificateNo,r.trainingDate,r.validUntil,daysTo(r.validUntil),derivedStatus(r),r.provider,r.documentLink,r.notes]);const csv=[header,...rows].map(row=>row.map(v=>`"${String(v??'').replaceAll('"','""')}"`).join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8;'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`SINSHE_SIO_Competency_${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(url)}
 return <Shell title="SIO, Learning & Competency" subtitle="Monitoring SIO, sertifikat kompetensi, masa berlaku dan training personel sesuai form aktual KPN Plantations.">
  <CompanyScopeBar filters={filters} onChange={setFilters} onReset={()=>setFilters(DEFAULT_COMPANY_FILTERS)}/>
  {notice&&<div style={{marginBottom:12,padding:'10px 12px',borderRadius:10,background:'#f0f8f3',border:'1px solid #d5e9da',color:'#176b34',fontSize:12,fontWeight:800}}>{notice}</div>}
  <div className="stats-grid four"><StatCard label="Compliance" value={`${compliance}%`} hint={`${valid} valid dari ${filtered.length}`} tone="green" icon={<GraduationCap/>}/><StatCard label="SIO / Certification" value={sioCount+certCount} hint={`${sioCount} SIO • ${certCount} sertifikat`} tone="blue" icon={<CheckCircle2/>}/><StatCard label="Due Soon" value={dueSoon} hint="≤30 hari" tone="orange" icon={<Clock3/>}/><StatCard label="Expired" value={expired} hint="perlu renewal" tone="red" icon={<AlertTriangle/>}/></div>
  <Panel title="Monitoring SIO & Sertifikat Kompetensi" action={`${filtered.length} record`} className="mt">
   <div style={{display:'grid',gridTemplateColumns:'2fr 1fr 1fr auto auto',gap:10,alignItems:'center',marginBottom:14}}><label style={{display:'flex',alignItems:'center',gap:8,border:'1px solid #dfe5e7',borderRadius:10,padding:'9px 11px',background:'#fff'}}><Search size={16}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Cari nama, NIK, jabatan, SIO, sertifikat..." style={{border:0,outline:0,width:'100%',fontSize:12}}/></label><select value={typeFilter} onChange={e=>setTypeFilter(e.target.value)} style={{padding:'10px',border:'1px solid #dfe5e7',borderRadius:10}}><option>All</option>{recordTypes.map(v=><option key={v}>{v}</option>)}</select><select value={statusFilter} onChange={e=>setStatusFilter(e.target.value)} style={{padding:'10px',border:'1px solid #dfe5e7',borderRadius:10}}><option>All</option><option>Valid</option><option>Due Soon</option><option>Expired</option></select><button onClick={exportCSV} style={{padding:'10px 12px',border:'1px solid #dfe5e7',borderRadius:10,background:'#fff',fontWeight:800,cursor:'pointer'}}><Download size={15} style={{verticalAlign:'middle',marginRight:6}}/>CSV</button>{canManage&&<button onClick={()=>setModal(true)} style={{padding:'10px 13px',border:0,borderRadius:10,background:'#0d6b3d',color:'#fff',fontWeight:850,cursor:'pointer'}}><Plus size={16} style={{verticalAlign:'middle',marginRight:6}}/>Tambah</button>}</div>
   <div className="table-wrap"><table><thead><tr><th>No.</th><th>PT</th><th>Nama Karyawan</th><th>NIK / Jabatan</th><th>Status Kerja</th><th>Jenis SIO/Sertifikat</th><th>No. Sertifikat</th><th>Tanggal Terbit</th><th>Berlaku Sampai</th><th>Sisa Hari</th><th>Status</th><th>Dokumen</th></tr></thead><tbody>{filtered.map((r,i)=><tr key={r.id}><td>{i+1}</td><td><b>{r.companyCode||'-'}</b></td><td><b>{r.employeeName}</b><br/><small>{r.unit||'-'}</small></td><td>{r.employeeId||'-'}<br/><small>{r.jobTitle||'-'}</small></td><td>{r.workStatus}</td><td><b>{r.trainingName}</b><br/><small>{r.recordType} • {r.category}</small></td><td>{r.certificateNo||'-'}</td><td>{fmt(r.trainingDate)}</td><td>{fmt(r.validUntil)}</td><td>{daysTo(r.validUntil)??'-'}</td><td><Badge tone={tone(derivedStatus(r))}>{derivedStatus(r)}</Badge></td><td>{r.documentLink?<a href={r.documentLink} target="_blank" rel="noreferrer"><ExternalLink size={16}/></a>:'-'}</td></tr>)}{!filtered.length&&<tr><td colSpan="12" style={{padding:24,textAlign:'center',color:'#7c858e'}}>Belum ada data aktual SIO/sertifikat pada filter ini. Form input sudah mengikuti template Monitoring SIO & Sertifikat.</td></tr>}</tbody></table></div><div style={{marginTop:14}}><Progress value={compliance} tone={compliance>=90?'green':'orange'}/></div>
  </Panel>
  {modal&&<div style={{position:'fixed',inset:0,background:'rgba(15,25,22,.52)',zIndex:1000,display:'grid',placeItems:'center',padding:20}} onMouseDown={e=>{if(e.target===e.currentTarget)setModal(false)}}><form onSubmit={save} style={{width:'min(820px,96vw)',maxHeight:'90vh',overflow:'auto',background:'#fff',borderRadius:16,padding:20,boxShadow:'0 24px 70px rgba(0,0,0,.25)'}}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:16}}><div><h2 style={{margin:0}}>Tambah SIO / Sertifikat / Training</h2><p style={{margin:'4px 0 0',color:'#7b858f',fontSize:12}}>Field mengikuti Monitoring SIO & Sertifikat Kompetensi Lingkungan.</p></div><button type="button" onClick={()=>setModal(false)} style={{border:0,background:'#f3f5f5',borderRadius:10,padding:8,cursor:'pointer'}}><X size={18}/></button></div><div style={{display:'grid',gridTemplateColumns:'repeat(2,minmax(0,1fr))',gap:12}}><Field label="Company / PT"><select value={form.companyCode} onChange={e=>update('companyCode',e.target.value)}>{COMPANY_MASTER.map(c=><option key={c.code} value={c.code}>{c.code} — {c.name}</option>)}</select></Field><Field label="Nama Karyawan"><input value={form.employeeName} onChange={e=>update('employeeName',e.target.value)}/></Field><Field label="NIK"><input value={form.employeeId} onChange={e=>update('employeeId',e.target.value)}/></Field><Field label="Jabatan"><input value={form.jobTitle} onChange={e=>update('jobTitle',e.target.value)}/></Field><Field label="Status Kerja"><select value={form.workStatus} onChange={e=>update('workStatus',e.target.value)}>{workStatuses.map(v=><option key={v}>{v}</option>)}</select></Field><Field label="Unit"><input value={form.unit} onChange={e=>update('unit',e.target.value)}/></Field><Field label="Record Type"><select value={form.recordType} onChange={e=>update('recordType',e.target.value)}>{recordTypes.map(v=><option key={v}>{v}</option>)}</select></Field><Field label="Jenis SIO / Sertifikat / Training"><input value={form.trainingName} onChange={e=>update('trainingName',e.target.value)}/></Field><Field label="Kategori"><select value={form.category} onChange={e=>update('category',e.target.value)}>{categories.map(v=><option key={v}>{v}</option>)}</select></Field><Field label="No. Sertifikat"><input value={form.certificateNo} onChange={e=>update('certificateNo',e.target.value)}/></Field><Field label="Tanggal Terbit / Training"><input type="date" value={form.trainingDate} onChange={e=>update('trainingDate',e.target.value)}/></Field><Field label="Berlaku Sampai"><input type="date" value={form.validUntil} onChange={e=>update('validUntil',e.target.value)}/></Field><Field label="Provider / Lembaga"><input value={form.provider} onChange={e=>update('provider',e.target.value)}/></Field><Field label="Kompetensi"><input value={form.competency} onChange={e=>update('competency',e.target.value)}/></Field><Field label="Link Dokumen"><input value={form.documentLink} onChange={e=>update('documentLink',e.target.value)} placeholder="https://..."/></Field><Field label="Catatan"><input value={form.notes} onChange={e=>update('notes',e.target.value)}/></Field><label style={{display:'flex',alignItems:'center',gap:8,fontSize:12,fontWeight:800}}><input type="checkbox" checked={form.mandatory} onChange={e=>update('mandatory',e.target.checked)}/> Mandatory / wajib</label></div><div style={{display:'flex',justifyContent:'flex-end',gap:8,marginTop:18}}><button type="button" onClick={()=>setModal(false)} style={{padding:'10px 14px',border:'1px solid #dfe5e7',borderRadius:10,background:'#fff',fontWeight:800}}>Batal</button><button type="submit" style={{padding:'10px 15px',border:0,borderRadius:10,background:'#0d6b3d',color:'#fff',fontWeight:850}}>Simpan Record</button></div></form></div>}
 </Shell>
}

function Field({label,children}){return <label style={{display:'grid',gap:5,fontSize:11,fontWeight:850,color:'#56616b'}}><span>{label}</span><div style={{display:'grid'}}>{children}</div><style jsx>{`input,select{width:100%;border:1px solid #dfe5e7;border-radius:9px;padding:10px 11px;font-size:12px;outline:none;background:#fff}`}</style></label>}
